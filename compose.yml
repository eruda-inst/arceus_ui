services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_BASE_HTTP_URL: ${NEXT_PUBLIC_BASE_HTTP_URL}
        NEXT_PUBLIC_BASE_WS_URL: ${NEXT_PUBLIC_BASE_WS_URL}
    container_name: arceus-ui
    env_file:
      - .env
    ports:
      - "3000:3000"
